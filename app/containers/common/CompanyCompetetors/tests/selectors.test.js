// import { fromJS } from 'immutable';
// import { makeSelectCompanyCompetetorsDomain } from '../selectors';

// const selector = makeSelectCompanyCompetetorsDomain();

describe('makeSelectCompanyCompetetorsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
