// import { fromJS } from 'immutable';
// import { makeSelectCompanyStatsDomain } from '../selectors';

// const selector = makeSelectCompanyStatsDomain();

describe('makeSelectCompanyStatsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
