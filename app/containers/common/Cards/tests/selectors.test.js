// import { fromJS } from 'immutable';
// import { makeSelectCardsDomain } from '../selectors';

// const selector = makeSelectCardsDomain();

describe('makeSelectCardsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
