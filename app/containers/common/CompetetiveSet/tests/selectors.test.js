// import { fromJS } from 'immutable';
// import { makeSelectCompetetiveSetDomain } from '../selectors';

// const selector = makeSelectCompetetiveSetDomain();

describe('makeSelectCompetetiveSetDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
