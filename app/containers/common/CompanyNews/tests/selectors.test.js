// import { fromJS } from 'immutable';
// import { makeSelectCompanyNewsDomain } from '../selectors';

// const selector = makeSelectCompanyNewsDomain();

describe('makeSelectCompanyNewsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
