// import { fromJS } from 'immutable';
// import { makeSelectCompanyPortfolioDomain } from '../selectors';

// const selector = makeSelectCompanyPortfolioDomain();

describe('makeSelectCompanyPortfolioDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
