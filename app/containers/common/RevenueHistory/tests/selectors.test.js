// import { fromJS } from 'immutable';
// import { makeSelectRevenueHistoryDomain } from '../selectors';

// const selector = makeSelectRevenueHistoryDomain();

describe('makeSelectRevenueHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
