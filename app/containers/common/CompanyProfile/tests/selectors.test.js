// import { fromJS } from 'immutable';
// import { makeSelectCompanyProfileDomain } from '../selectors';

// const selector = makeSelectCompanyProfileDomain();

describe('makeSelectCompanyProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
