// import { fromJS } from 'immutable';
// import { makeSelectSocialStataticsDomain } from '../selectors';

// const selector = makeSelectSocialStataticsDomain();

describe('makeSelectSocialStataticsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
