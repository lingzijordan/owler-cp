// import { fromJS } from 'immutable';
// import { makeSelectWebsiteHistoryDomain } from '../selectors';

// const selector = makeSelectWebsiteHistoryDomain();

describe('makeSelectWebsiteHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
