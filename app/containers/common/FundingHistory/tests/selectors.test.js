// import { fromJS } from 'immutable';
// import { makeSelectFundingHistoryDomain } from '../selectors';

// const selector = makeSelectFundingHistoryDomain();

describe('makeSelectFundingHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
