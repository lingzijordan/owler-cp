// import { fromJS } from 'immutable';
// import { makeSelectEmployeeHistoryDomain } from '../selectors';

// const selector = makeSelectEmployeeHistoryDomain();

describe('makeSelectEmployeeHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
